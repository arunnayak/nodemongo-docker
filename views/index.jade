extends layout

block content
  h1= title
  p Welcome to docker #{title} app
  ul
    for todo in todos
      li.listItem(class=todo.complete ? "completed": "")
        | Description: #{todo.description}
        | - Completed: #{todo.complete}
    
    else
      p no todos found